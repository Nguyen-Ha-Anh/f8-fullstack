
    /*document.writeln('<h1>hello anh em</h1>')

    console.log('<h1>hello anh em</h1>')
    console.log('1234', 54322)
    console.log(1+2, 3-6, 3*6, -9/0)

    console.log('hello' + ' anh em')
    console.log('2'+ 2000)

    // this is comment
    /*
    *this is comment
    * hello anh em
    * 
    
    /*
    * Bien: variable
    * use to store any temp value
    * store to RAM
    * reset -> remove
    * declear a variable -> 4 ways
    * var (<2015)
    * let / const (>2015 - EC6)
    * */

    /*var a 
    a = 1

    //inital variable
    var b = 1

    // initial variable
    var b = 1
    let c = 1
    const d = 1

    a = a + 1
    c = a + b
    // d = d + 1
    console.log(a, b)

    /*
    *oparator + - * /
    *number + number = number
    *number - number = number
    *number * number = number
    *number / number = number

    *string + string = string
    *string - string = NaN ( Not a number)
    *string * string = NaN (NOt a number)
    *string / string = NaN (NOt a number)

    *String link number
    *string + string = string
    *string - string = number
    *string * string = number
    *string / string = number
    
    (*) JS try to convert your operator to corect operator -> should not depend on it 
    * */

    /* 
    * a ** 2 = a * a
    * a ** 3 = a * a * a
    **/
   // lấy dư: %
//    console.log (10 % 2)
   
//    let a = 5
    //  a ++
    // //  ++ a
    // const b = a + ++a + a++ + ++a + a-- - --a

    // a = a + 5 // a += 5

    // console.log(a++) 


    /*
    *comparison operator
    * == / === (*)
    *  >
    *  <
    * >=
    * <=
    * != / !==
    * ? notsure
    **/

    /*
    * data type
    * string
    * number
    * bool
    * null / undefine / unknow /
    * object
    * function
    * 
    * to check data type: typeof(<variable)
    **/

    // console.log(3===2)

    // const a = undefined
    // console.log(typeof a)

    /*
    * Concat: convert data type
    *
    * DataType(<variable)
    * 
    * string<-> number -> ok
    * string -> bool
    * length of string > 0 ----> true, else ->>> false
    * 
    * number -> bool
    * 0 -> false else true
    * 
    * bool -> string
    * true -> 'true'
    * false -> 'false'
    * 
    * bool -> number
    * true -> 1
    * false -> 0
    **/

    // let là hàm chữ
    // let a = -1
    // a = Boolean(a)
    // console.log(a)

    // === <-> !==

    // thêm ! để đảo ngược false thành true

    // string rỗng => false

    //if sẽ chạy đến khi tìm được code hợp lệ - chỉ 1 điều kiện hợp lệ

    //case là chạy hết code, tìm ra nhiều hơn 1 điều kiện hợp lệ

    // default chạy lại dòng code đó

    // switch (a % 3) {
    //     case 1:
    //         console.log('du 1')
    //         break
    //     case 2:
    //         console.log('du 2')
    //         break
    //     case 3:
    //         console.log('du 3')
    //         break
    //     default:
    //         console.log('hihi')
        
    // }

    // const a = 5

    // if (a % 2 === 0) console.log('a chan')
    // else console.log('a le')

    // chia 3 chu 0 -> du 0
    // chia 3 chu 1 -> du 1
    // chia 3 chu 1 -> du 3

    // if (a % 3 === 0) console.log('du 0')
    // else if (a % 3 === 1) console.log('du 1')
    // else console.log('du 2')

    // const a = 1, b = -2, c = 1
    // delta = b**2 - 4*a*c

    // if(delta < 0) console.log('vn')
    //     else if (delta === 0) {
    //     const x = -b / (2*a)
    // }
    // else {
    //     x1 = (-b - delta**(1/2)/(2*a))
    //     x2 = (-b + delta**(1/2)/(2*a))
    //     console.log(x1, x2)
    // }

    /*
    * Loop: re-use code
    * for
    *   for
    *   for of
    *   forEach
    *   map
    *   filter
    *   reduce
    * 
    *   while
    * 
    * do while ( do not use)
    **/

    /*
    * syntax
    *
    * for (init; condition; next step) {
    * block
    * }
    **/

    const a = 7
    if (a < 1)
        console.log(false)
    for (let a > 1)
